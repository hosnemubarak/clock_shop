# =============================================================================
# Clock Shop - cPanel Production Environment Configuration
# =============================================================================
# Copy this file to .env and update with your actual values
# =============================================================================

# =============================================================================
# SECURITY SETTINGS (REQUIRED - MUST CHANGE!)
# =============================================================================

# Generate a new secret key using:
# python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=CHANGE-THIS-TO-A-LONG-RANDOM-SECRET-KEY

# MUST be False in production
DEBUG=False

# Add your domain(s) - comma-separated
ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com

# Add your domain(s) with https:// - comma-separated
CSRF_TRUSTED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# =============================================================================
# MYSQL DATABASE SETTINGS (REQUIRED)
# =============================================================================
# Get these values from cPanel MySQL Databases section

DB_ENGINE=mysql
DB_NAME=rumaelec_clock_shop_db
DB_USER=rumaelec_clock_shop_user
DB_PASSWORD=your_database_password_here
DB_HOST=localhost
DB_PORT=3306

# =============================================================================
# BUSINESS SETTINGS
# =============================================================================

SHOP_NAME=Clock Shop
CURRENCY_SYMBOL=৳
LOW_STOCK_THRESHOLD=5

# Role-Based Access Control
# Set to False to disable strict Manager/Admin/Cashier group checks
# and allow any logged-in user full access to the system.
ENABLE_RBAC=True

# =============================================================================
# NOTES
# =============================================================================
# 1. Replace 'username' in DB_NAME and DB_USER with your cPanel username
# 2. Use the full database name as shown in cPanel (e.g., cpanelusername_clock_shop_db)
# 3. Generate a strong SECRET_KEY - never use the default!
# 4. Set DEBUG=False for production
# 5. Update ALLOWED_HOSTS and CSRF_TRUSTED_ORIGINS with your actual domain
# 6. Keep this file secure - never commit it to version control
# =============================================================================
